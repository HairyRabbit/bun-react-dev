{
  "name": "bun-react-dev",
  "version": "0.0.1",
  "scripts": {
    "build:runtime": "bun build ./runtime/index.ts --outdir . --target browser --format=esm --entry-naming=runtime.js --external react-refresh --sourcemap=inline",
    "postinstall": "bun run build:runtime"
  },
  "devDependencies": {
    "@types/node": "^20.4.6",
    "@types/react-refresh": "^0.14.0",
    "@types/web": "^0.0.112",
    "bun-types": "^0.7.1",
    "react": "^18.2.0",
    "react-refresh": "^0.14.0",
    "react-refresh-typescript": "^2.0.9",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "react-dom": "^18.2.0"
  }
}
